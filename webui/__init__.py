"""WebUI subpackage — small reusable widgets and helpers.

Most of the pipeline UI still lives in herbarium_pipeline_webui.py. This
package collects the pieces that are useful in isolation: file picker,
data-source list, section/accordion/pill helpers, and the standard
label+input+browse row pattern. Everything in here is presentational
and depends only on NiceGUI.
"""
